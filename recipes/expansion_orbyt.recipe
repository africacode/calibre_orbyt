from calibre.web.feeds.recipes import BasicNewsRecipe

class ExpansionRecipe(BasicNewsRecipe):
    title = 'Expansión'
    oldest_article = 7
    max_articles_per_feed = 100
    language = 'es'
    feeds = [
        ('Orbyt', 'https://www.expansion.com/orbyt/')
    ]
    # Añade aquí la lógica si usas un plugin custom para Orbyt.

